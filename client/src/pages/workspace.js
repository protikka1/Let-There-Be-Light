import React from 'react';

const Workspace = () => (
  <div>
    <h2>Employee & Volunteer Workspace</h2>
    <p>Login to access internal tools, schedules, and resources.</p>
    {/* Future: secure login, dashboards */}
  </div>
);

export default Workspace;
